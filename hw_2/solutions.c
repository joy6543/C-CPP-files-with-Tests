#include "solutions.h"
#include <math.h>

int running_total (a) {
  int i=0;
  for(i=0; i<a; i++){
    a = i + a;
  }
  return a;
}

int reverse (x,a) {
  
  return a;
}

int reverse_in_place (x,a) {

  return a;
}

int num_occurences (a,len,num) {
  int a[] = { 1, 1, 2, 3, 1, 4, 5, 2, 20, 5 };

  return num;
}
*/